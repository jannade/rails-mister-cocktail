<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.1)), url(https://static.vinepair.com/wp-content/uploads/2016/05/fresh-herb-cocktails-2.jpg)">
</div>

<div class="container">
  <div class="right-btn">
    <%= link_to "Add ingredient", new_cocktail_dose_path(@cocktail.id), class: "btn-ghost" %>
    <%= link_to "Back to all", cocktails_path, class: "btn-ghost" %>
  </div>

  <h1><%= @cocktail.name  %></h1>

  <ul>
      <% @doses.each do |dose| %>
        <li>
          <p><%= dose.description %> of <%= @ingredients.find(dose.ingredient_id).name %> </p>
        </li>
      <% end  %>
  </ul>
</div>


